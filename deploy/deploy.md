### Plugins deployment policy

Only first-party custom plugins are deployed.

Currently deployed:
- dx-client-portal
- pricing-calculator

Third-party plugins are managed directly on the live environment
and are never overwritten by deploy scripts.